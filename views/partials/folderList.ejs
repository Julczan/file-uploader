<% if (locals.folders && folders.length !== 0) {%>
<ul>
  <% folders.forEach((folder) => { %>
  <li>
    <a href="/folders/<%= folder.id %>"> <%= folder.title %></a>
  </li>
  <% }); %>
</ul>
<% }else{ %>
No folders yet
<% } %>
