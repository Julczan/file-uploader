<div>
  <button popovertarget="create-folder-dialog">Create folder</button>

  <dialog id="create-folder-dialog" popover>
    <div class="form-container">
      <h2 class="form-title">Create new Folder</h2>
      <div class="form-errors"><%- include("errorMessages.ejs") %></div>
      <form
        action="/folders/create/<%= locals.openedFolderId %>"
        method="post"
        class="auth-form"
      >
        <div class="form-group">
          <label for="folderTitle">Folder title</label>
          <input id="folderTitle" name="folderTitle" type="text" required />
        </div>
        <button type="submit" class="btn-submit">Create</button>
        <button
          popovertarget="create-folder-dialog"
          popovertargetaction="hide"
          type="button"
        >
          Close
        </button>
      </form>
    </div>
  </dialog>
</div>
